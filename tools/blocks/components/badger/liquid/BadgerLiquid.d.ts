/**
 * Represents the configuration for a liquid block in Minecraft Legends.
 *
 * This interface specifies properties related to liquid behavior, such as flow dynamics and interactions.
 * It allows developers to define custom liquid blocks with specific properties.
 * @interface BadgerLiquid
 * @category Block Configuration
 * @remarks
 * Properly configuring this setting ensures that liquid blocks behave consistently and effectively.
 * Misconfigured settings may lead to visual or functional inconsistencies.
 */
interface BadgerLiquid {}
