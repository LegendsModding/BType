/**
 * Contains visibility states for claimed area map indicators.
 * @readonly
 * @remarks
 * Valid states:
 * - VISIBLE: Areas shown on map
 * - INVISIBLE: Areas hidden from map
 */
export const MAP_ClaimedAreaVisibility = {
  VISIBLE: "visible",
  INVISIBLE: "invisible",
};
