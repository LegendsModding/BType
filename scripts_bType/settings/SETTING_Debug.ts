/**
 * Debug configuration settings container.
 * @readonly
 * @remarks Contains debug-specific flags. TelemetryEnabled controls analytics transmission. Should not be modified in production builds.
 */
export const SETTING_Debug = {
  telemetryEnabled: 1,
};
