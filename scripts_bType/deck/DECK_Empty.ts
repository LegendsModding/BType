/**
 * Creates a new empty deck structure.
 * @returns {Deck} Empty card container
 * @example
 * const newDeck = DECK_Empty();
 * @description
 * Use cases:
 * - Base for new decks
 * - Temporary storage
 * - Generation resets
 */
export const DECK_Empty = (): Deck => {
  return [];
};
