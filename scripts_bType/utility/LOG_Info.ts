/**
 * Logs an informational message.
 * @param message The message to log.
 * @param data Optional additional data.
 */
export const LOG_Info = (message: LogMessage, data?: string): void => {
  //OUTPUT_DebugLog(message, 1);
  if (data) {
    //OUTPUT_DebugLog(data, 1);
  }
};
