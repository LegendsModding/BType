/**
 * BSharp placement system configuration parameters.
 * @readonly
 * @remarks
 * Contains special values for placement rule overrides. requireAll (-1) indicates all conditions must be met, maxDistance (-1) represents unlimited range.
 */
export const BSHARP_Placement = {
  requireAll: -1,
  maxDistance: -1,
};
