/**
 * Resource management ticket type identifiers.
 * @readonly
 * @remarks
 * Defines categories for population cap systems: build (construction), gather (collection), spawn (entity generation).
 */
export const TICKET: TicketTypes = {
  build: "build",
  gather: "gather",
  spawn: "spawn",
};
