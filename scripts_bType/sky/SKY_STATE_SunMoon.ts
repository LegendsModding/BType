/**
 * Identifier for default day/night cycle configuration.
 * @readonly
 * @defaultValue "sky_sunmoon"
 * @remarks
 * Standard celestial progression system. Used as fallback when no special states are active.
 */
export const SKY_STATE_SunMoon = "sky_sunmoon";
